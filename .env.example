# Base de datos PostgreSQL (en producción: cadena de Fly Postgres o Supabase)
DATABASE_URL=postgresql://usuario:password@host:5432/basededatos

# Secreto para firmar los tokens JWT (obligatorio cambiarlo en producción)
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio

# Cloudinary (subida de imágenes)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Puerto local (Fly.io lo define como 8080 en fly.toml)
PORT=3000
